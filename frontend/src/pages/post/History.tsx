// Post — History: every published/scheduled post and its per-platform outcome.
import { PostHistoryView } from "@/components/post/PostList";

export default function History() {
  return <PostHistoryView />;
}

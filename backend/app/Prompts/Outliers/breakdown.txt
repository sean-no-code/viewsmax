You are a short-form/long-form video strategist. Break down why the following {platform} video performed, so a creator can replicate its format.

VIDEO
Title: {title}
Creator: {channel}
Platform: {platform}
Duration (seconds): {duration_seconds}
Views: {views}

TRANSCRIPT (timestamped where available)
{transcript}

Respond with ONLY a JSON object — no prose, no markdown fences — in exactly this shape:

{
  "idea": {
    "topic": "<the broad topic, 3-6 words>",
    "idea_seed": "<1-2 sentences: the core idea and why it carries the video>",
    "unique_angle": "<1-2 sentences: what makes this take different / contrarian / first-mover>"
  },
  "hook": [
    {"time": "0.0s", "line": "<exact opening line or visual beat>", "note": "<why this beat retains, 1-2 sentences>"}
    // 2-4 beats covering roughly the first 3 seconds, each with a time offset like "1.4s"
  ],
  "structure": {
    "summary": "<e.g. '5 beats over 38s'>",
    "beats": [
      {
        "label": "<UPPERCASE one-word label, e.g. HOOK / FLIP / ESCALATION / RECEIPTS / PAYOFF>",
        "start": "<m:ss, e.g. '0:00' — never raw seconds>",
        "end": "<m:ss, e.g. '0:03'>",
        "pct": <integer share of runtime, all beats must sum to 100>,
        "title": "<e.g. 'Hook — stat shock'>",
        "note": "<what happens in this beat and why it works, 1-2 sentences>",
        "highlight": <"red" for the hook beat, "volt" for the payoff/loop beat, null otherwise>
      }
      // 3-6 beats in chronological order
    ]
  },
  "visual": [
    {"title": "<pattern, e.g. 'Talking head · 40% of runtime'>", "note": "<how the frame is built and why, 1-2 sentences>"}
    // 3-5 cards; infer conservatively from the transcript and platform norms — never invent specifics you cannot support
  ],
  "transcript": [
    {"time": "<m:ss>", "text": "<transcript passage>", "label": <a short annotation like "hook", "reframe", "receipts", "loop point" for the notable passages, null for the rest>}
    // Group the transcript into readable passages of 1-3 sentences each, preserving order and covering the whole video
  ]
}

Rules:
- Ground every claim in the transcript; where the transcript is thin, reason from the title and platform norms and stay conservative.
- Keep notes tight and tactical — written for a creator deciding what to copy.
- "pct" values are integers summing to 100.
- Output valid JSON only.
